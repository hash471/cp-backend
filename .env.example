# Application
NODE_ENV=development
PORT=3000

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=complaints
DB_SSL=false

# Authentication (comma-separated username:password pairs)
AUTH_CREDENTIALS=admin:admin123,officer:officer123
